import nextVitals from 'eslint-config-next/core-web-vitals'

/** ESLint configuration for Next.js, React, and TypeScript source files. */
const eslintConfig = [...nextVitals]

export default eslintConfig
